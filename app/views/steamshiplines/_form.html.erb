<%= form_with(model: steamshipline, local: true) do |form| %>
  <% if steamshipline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(steamshipline.errors.count, "error") %> prohibited this steamshipline from being saved:</h2>

      <ul>
        <% steamshipline.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :country_id %>
    <%= form.collection_select(:country_id, @countries, :id, :country, include_blank: false) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
